fn main() {
    // Listening to the websocket
    // If it found that somebody mines a block, try to insert that into blockchain

    // blockchain.update_with_block(block_from_the_internet);

    println!("This is validator node!!");
}
